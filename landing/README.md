# Landing page

This static page uses synthetic product data. It contains no dashboard export, session record, user path, host name, or experiment artifact.

Preview it locally:

```bash
python3 -m http.server 4173 --directory landing
```

Then open <http://127.0.0.1:4173>.
