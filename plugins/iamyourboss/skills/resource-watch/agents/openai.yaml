interface:
  display_name: "Resource Watch"
  short_description: "Bind local and SSH compute to one session"
  brand_color: "#0891B2"
  default_prompt: "Use $resource-watch to attach this experiment process to my supervised session."
